//when we have lots of component then we have to export each and have to access each in app jsx file to prevent this we made a file index.js and import all component in it and then export only one file 
import InputBox from "./InputBox"

export {InputBox}